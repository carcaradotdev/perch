package com.example.sample.di

/**
 * The marker every app-lifetime binding is scoped to. Metro takes any class as a scope, so this one
 * belongs to the app; Perch knows nothing about it.
 */
public object AppScope
